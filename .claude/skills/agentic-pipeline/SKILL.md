---
name: agentic-pipeline
description: >
  Generate a complete simulation setup — scene (objects), task (+ robot placement), and
  environment (background/table/lighting/cameras) — from a natural-language description, with
  deformable objects as first-class citizens. Also rearranges existing generated scenes
  (--scene_init). Use when the user wants to create a scene, task, or full demo setup.
---

Read `agentic_pipeline/SKILL.md` (repo root) and follow it exactly — it is the authoritative
skill document (kept next to the pipeline code it drives).
