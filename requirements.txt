# Reproducible single-venv dependency set for RoboLab_VBD.
#
# Install with the uv command in README.md. This intentionally does not use
# newton[examples] or newton[importers] directly because RoboLab/IsaacLab pins
# pyglet and trimesh versions that conflict with those Newton extras.

# Build/runtime tooling compatible with IsaacSim's packaging pin.
setuptools<81
wheel<0.45
packaging==23.0

# Compatibility pins chosen by the RoboLab/IsaacLab stack.
pyglet==1.5.31
trimesh==4.5.1

# CUDA 12.8 runtime stack. These require the PyTorch/NVIDIA indexes in README.md.
torch==2.7.0+cu128
torchvision==0.22.0+cu128
torchaudio==2.7.0+cu128
warp-lang==1.15.0.dev20260610

# Local editable projects. Populate _external/ first; see README.md.
-e ./_external/newton[sim]
-e ./_external/RoboLab[all]

# Newton USD viewer and asset-loading dependencies used by RoboLab_VBD examples.
PyYAML==6.0.2                 # parse the repo-root settings.yaml (robot selection + render defaults)
GitPython==3.1.50
usd-core==26.3
newton-usd-schemas==0.3.1
resolve-robotics-uri-py==0.4.1
meshio==5.3.5
pycollada==0.9.3
fast-simplification==0.1.13
alphashape==1.3.1
coacd==1.0.11

# Grasp-candidate generation (deformableManipulationTools/grasp_passes/geometric). Both are PyPI
# releases, NOT the _external/skeletor checkout: _external/ is disposable by CLAUDE.md rule, so a
# runtime import must never resolve from it.
scikit-image==0.26.0          # skimage.morphology.skeletonize — voxel medial-axis fallback for
                              # non-watertight meshes (most YCB scans)
skeletor==1.7.1               # skeletor.skeletonize.by_wavefront — mesh-native medial axis
