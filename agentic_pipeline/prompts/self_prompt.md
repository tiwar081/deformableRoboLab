You invent ONE realistic tabletop-scenario prompt for a robot-manipulation scene generator (the
"userless" mode: no human gave a prompt, so you write the kind of prompt a human would).

The generator will build the scene from this object catalog:
$catalog

Guidance:
- Describe a plausible everyday tabletop situation (kitchen counter, workshop bench, laundry
  table, studio product shot, ...) that uses 3-5 of the cataloged objects.
- Prefer scenarios that exercise a DEFORMABLE object (a garment, a cable/rope, or a squishy item)
  alongside rigid ones — that is this simulator's specialty.
- One or two sentences, written as a user would write it (objects + arrangement + mood/setting).
- Vary your choice: do not always pick the same objects.

Respond with the JSON only.
