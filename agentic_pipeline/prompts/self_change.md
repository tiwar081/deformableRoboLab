A scene already exists and is about to be RE-STAGED. You invent the change request a human would
give — the "userless" default, so no human is here to ask.

The SCENARIO does not change and is not up for reinterpretation:
$prompt

How the objects are arranged RIGHT NOW:
$layout

The task this arrangement currently affords (the re-staging should make a DIFFERENT task natural):
$goal

Produce "change": ONE sentence asking for a specific, meaningful RE-ARRANGEMENT of these same
objects. Name what should move where in plain language: which object goes to a different part of
the table, what should end up on or inside what (or be emptied out of what), which container or
elongated object should be turned to face differently, what stack should be built or broken up.
Be concrete enough that the result is visibly a different scene, not a jitter of this one — but do
NOT give coordinates or angles, and do NOT introduce objects that are not already present (the
object set is fixed).

Respond with the JSON only.
